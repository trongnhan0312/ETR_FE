# 🚀 PRODUCTION BUILD (tự động chạy trên Vercel khi push lên git)
# File này COMMIT lên git — Vercel clone repo và chạy `npm run build` (mode=production)
# nên sẽ đọc đúng giá trị VITE_API_URL_DEPLOY này.
# (VITE_* là giá trị public, không phải secret nên an toàn khi commit.)

# API backend đã deploy — frontend trên Vercel CHỈ gọi URL này, không gọi localhost.
VITE_API_URL_DEPLOY="https://etrmanagement-be-fwhvagaxf3f3dmf0.southeastasia-01.azurewebsites.net/api"
